---
const cases = [
  {
    client: 'CloudWorks',
    industry: 'IT-dienstverlening',
    headline: 'Van brochurewebsite naar leadmachine',
    result: '+212% gekwalificeerde leads in 6 maanden',
    description:
      'Replatforming naar Astro, implementatie van marketing automation en SEO/SEA campagnes gericht op enterprise leads.',
  },
  {
    client: 'FitNation',
    industry: 'Lifestyle & health',
    headline: 'Community platform met 4x engagement',
    result: '+47% groei in memberships',
    description:
      'Design system ontwikkeld, headless CMS gekoppeld en app-integraties voor een schaalbare merkervaring.',
  },
  {
    client: 'LegalFlow',
    industry: 'Juridisch',
    headline: 'Conversiegedreven funnel voor leads op maat',
    result: '-62% leadkosten via SEA en SEO',
    description:
      'Nieuwe merkidentiteit, CRO-framework en dashboards voor realtime inzicht in de pijplijn.',
  },
];
---

<section id="cases" class="relative py-28">
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_rgba(12,10,29,0.85),_rgba(12,10,29,0.2))]"></div>
  <div class="absolute inset-0 grid-pattern opacity-10"></div>
  <div class="section-gradient section-gradient--violet parallax-bg" style="top:-25%;left:-18%;"></div>
  <div class="section-gradient section-gradient--cyan" style="bottom:-30%;right:-12%;"></div>

  <div class="relative mx-auto max-w-6xl px-6">
    <div class="mb-16 flex flex-col gap-8 md:flex-row md:items-end md:justify-between">
      <div class="space-y-6 md:max-w-3xl">
        <span class="section-eyebrow">Cases</span>
        <h2 class="headline-underline text-pretty text-3xl font-semibold text-white md:text-4xl" data-animate="underline" data-split>
          Projecten die laten zien wat strategie + uitvoering oplevert.
        </h2>
        <p class="text-balance text-lg text-white/70">
          Van scale-ups tot ambitieuze MKB-organisaties: we bouwen aan digitale ecosystemen die duurzaam resultaat opleveren.
        </p>
      </div>
      <a href="#contact" class="btn-secondary">Plan een sessie</a>
    </div>

    <div class="relative">
      <div class="motion-stripe"></div>

      <div class="grid gap-8 md:grid-cols-3">
        {cases.map((item) => (
          <article class="glass-panel glow-ring tilt-card relative flex h-full flex-col justify-between gap-8 p-8 transition duration-500 hover:-translate-y-2" data-tilt data-tilt-max="7" data-tilt-speed="500" data-tilt-perspective="1100" data-tilt-glare="true" data-tilt-max-glare="0.25">
            <div class="relative space-y-6">
              <div class="flex items-center justify-between text-xs font-semibold uppercase tracking-[0.35em] text-white/60">
                <span>{item.client}</span>
                <span>{item.industry}</span>
              </div>
              <h3 class="text-pretty text-2xl font-semibold text-white">{item.headline}</h3>
              <p class="text-sm text-white/70">{item.description}</p>
            </div>
            <div class="flex items-center justify-between text-sm font-semibold text-brand-200">
              <span>{item.result}</span>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
                <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
            </div>
          </article>
        ))}
      </div>
    </div>
  </div>
</section>
