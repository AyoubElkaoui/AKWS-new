---
const cases = [
  {
    client: 'CloudWorks',
    industry: 'IT-dienstverlening',
    headline: 'Van brochurewebsite naar leadmachine',
    result: '+212% gekwalificeerde leads in 6 maanden',
    description:
      'Replatforming naar Astro, implementatie van marketing automation en SEO/SEA campagnes gericht op enterprise leads.',
  },
  {
    client: 'FitNation',
    industry: 'Lifestyle & health',
    headline: 'Community platform met 4x engagement',
    result: '+47% groei in memberships',
    description:
      'Design system ontwikkeld, headless CMS gekoppeld en app-integraties voor een schaalbare merkervaring.',
  },
  {
    client: 'LegalFlow',
    industry: 'Juridisch',
    headline: 'Conversiegedreven funnel voor leads op maat',
    result: '-62% leadkosten via SEA en SEO',
    description:
      'Nieuwe merkidentiteit, CRO-framework en dashboards voor realtime inzicht in de pijplijn.',
  },
];
---

<section id="cases" class="orb-section" data-variant="cases">
  <span class="orb-bubble" style="--orb-x: 12%; --orb-y: -18%; --orb-size: 44rem; --orb-color: rgba(126, 68, 255, 0.26);"></span>
  <span class="orb-bubble" style="--orb-x: 86%; --orb-y: 12%; --orb-size: 34rem; --orb-color: rgba(46, 130, 255, 0.2);"></span>
  <span class="orb-bubble" style="--orb-x: 52%; --orb-y: 108%; --orb-size: 50rem; --orb-color: rgba(90, 60, 240, 0.18); --orb-opacity: 0.42;"></span>

  <div class="orb-shell">
    <div class="orb-intro">
      <span class="orb-tag">Cases</span>
      <h2 class="orb-title">Projecten die laten zien wat strategie + uitvoering oplevert.</h2>
      <p class="orb-copy">
        Van scale-ups tot ambitieuze MKB-organisaties: we bouwen aan digitale ecosystemen die duurzaam resultaat opleveren.
      </p>
      <div class="orb-meta">
   <a href="/contact" class="hero-btn hero-btn--ghost">Plan een sessie</a>
      </div>
    </div>

    <div class="orb-grid orb-grid--cases">
      {cases.map((item) => (
        <article class="orb-card orb-card--case">
          <div class="orb-case__meta">
            <span>{item.client}</span>
            <span>{item.industry}</span>
          </div>
          <div class="orb-card__content">
            <h3 class="orb-card__title">{item.headline}</h3>
            <p class="orb-card__body">{item.description}</p>
          </div>
          <div class="orb-case__result">
            <span>{item.result}</span>
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
              <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
