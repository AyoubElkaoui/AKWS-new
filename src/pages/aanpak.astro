---
import Layout from '../layouts/Layout.astro';
import Header from '../components/nav/Header.astro';
import Footer from '../components/nav/Footer.astro';
import IconBadge from '../components/ui/IconBadge.astro';
const siteUrl = Astro.site?.toString() ?? 'https://akwebsolutions.nl';
const pageUrl = `${siteUrl}/aanpak`;

const approachStats = [
  { value: '2 weken', label: 'Sprintcadans' },
  { value: '4 fases', label: 'Van idee tot groei' },
  { value: '100%', label: 'Transparantie op KPI’s' },
];

const approachHighlights = [
  {
    icon: { set: 'heroicons-outline', name: 'sparkles' },
    title: 'Discovery met impact',
    body: 'Data, interviews en doelen in één sessie. We bepalen kansen voordat we pixels maken.',
  },
  {
    icon: { set: 'heroicons-outline', name: 'rectangle-stack' },
    title: 'Design dat launcht',
    body: 'UX, copy en UI in modulair design. Klaar voor development zonder extra rondes.',
  },
  {
    icon: { set: 'heroicons-outline', name: 'bolt' },
    title: 'Ship, learn, repeat',
    body: 'Geen big-bang. We releasen elke sprint en sturen bij op data en feedback.',
  },
];

const approachTimeline = [
  {
    step: 'Week 1',
    title: 'Kick-off orbit',
    body: 'Ambities, KPI’s en quick wins scherp. We delen het plan voor sprint één binnen 48 uur.',
  },
  {
    step: 'Week 2-3',
    title: 'Design + prototype',
    body: 'Flows, copy en visueel design testen we met stakeholders. Feedback gaat direct de volgende sprint in.',
  },
  {
    step: 'Week 4-5',
    title: 'Build + QA',
    body: 'Astro/Next.js build, koppelingen en performance-checks. Alles klaar om live te gaan.',
  },
  {
    step: 'Week 6+',
    title: 'Launch & growth',
    body: 'Campagnes live, dashboards up-to-date. Elke sprint levert learnings en volgende experimenten.',
  },
];

const phases = [
  {
    title: 'Discover',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.46)',
    description: 'Kick-off, data research, doelgroep-interviews en concurrentiescan. Alles voor een scherpe roadmap.',
    deliverables: ['Roadmap & planning', 'Hypotheses & KPI’s', 'Content & SEO-structuur'],
  },
  {
    title: 'Design',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.46)',
    description: 'UX flows, copywriting en high-end UI design met prototypes die we valideren met stakeholders.',
    deliverables: ['UX flows & wireframes', 'Design system', 'Storytelling & copy'],
  },
  {
    title: 'Build',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.46)',
    description: 'Development in Astro/Next.js, koppelingen naar CRM/marketingtools en performance optimalisatie.',
    deliverables: ['Modulaire componenten', 'CMS & integraties', 'Performance & QA'],
  },
  {
    title: 'Grow',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.46)',
    description: 'Launch, analytics, SEO/SEA, automation en doorlopende CRO-sprints.',
    deliverables: ['Campagnes & automation', 'Testing & optimalisatie', 'Dashboards & rapportage'],
  },
];

const guarantees = [
  {
    title: 'Transparante sprintplanning',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.38)',
    body: 'Elke sprint heeft duidelijke deliverables en een vast aanspreekpunt. Zo weet je precies wat er live gaat en wanneer.',
  },
  {
    title: 'Één multidisciplinair team',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.38)',
    body: 'Strategie, design, development en marketing werken synchroon. Geen overdrachten of ruis, wel snelheid.',
  },
  {
    title: 'Meetbare voortgang',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.38)',
    body: 'We sturen op KPI’s en dashboards. Iedere sprint krijg je inzichten, learnings en heldere next steps.',
  },
];

const rhythm = [
  {
    label: 'Kick-off orbit',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.32)',
    title: 'Co-creatie sessie met je kernteam',
    body: 'We leggen ambities vast, verzamelen data en prioriteren quick wins. Iedereen weet direct wat sprint één oplevert.',
  },
  {
    label: 'Validatie loops',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.32)',
    title: 'Design review & prototype test',
    body: 'We testen flows met stakeholders en passen messaging aan op inzichten. Feedback komt in een gedeeld board dat we strak opvolgen.',
  },
  {
    label: 'Launch control',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.32)',
    title: 'Release + marketing automation',
    body: 'Na QA draaien we de switch om, activeren campagnes en zorgen voor meetbare dashboards die direct gevuld worden.',
  },
  {
    label: 'Growth cadence',
    tone: 'violet',
    glow: 'rgba(122, 70, 255, 0.32)',
    title: 'Maandelijkse optimalisatie sprint',
    body: 'SEO, CRO en campagne-automatisering in vaste ritmes. Elke sprint levert nieuwe learnings, experimenten en revenue kansen op.',
  },
];

const faqs = [
  {
    question: 'Hoeveel tijd vraagt een traject van ons team?',
    answer: 'We werken in sprints van twee weken met vaste contactmomenten. Je hebt één contactpersoon die alle voorbereiding doet, zodat jouw tijdsbesteding gemiddeld twee uur per week is.',
  },
  {
    question: 'Kunnen we later opschalen of afschalen?',
    answer: 'Ja. Na elke sprint evalueren we de resultaten en bepalen we samen of we opschalen met extra experimenten of tijdelijk focussen op optimalisatie. Zo blijft de aanpak flexibel.',
  },
  {
    question: 'Werken jullie ook met bestaande teams of leveranciers?',
    answer: 'We sluiten aan op interne teams en bestaande tooling. We plannen gezamenlijke sessies, delen dashboards en gebruiken jouw project- of marketingstack waar mogelijk.',
  },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((item) => ({
    '@type': 'Question',
    name: item.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: item.answer,
    },
  })),
};

const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    {
      '@type': 'ListItem',
      position: 1,
      name: 'Home',
      item: siteUrl,
    },
    {
      '@type': 'ListItem',
      position: 2,
      name: 'Aanpak',
      item: pageUrl,
    },
  ],
};

const howToSchema = {
  '@context': 'https://schema.org',
  '@type': 'HowTo',
  name: 'Aanpak digitale trajecten AK Web Solutions',
  description: 'Zo bouwen we in vier fases aan digitale groei voor jouw organisatie.',
  totalTime: 'P8W',
  step: phases.map((phase) => ({
    '@type': 'HowToStep',
    name: phase.title,
    text: phase.description,
    itemListElement: phase.deliverables.map((item) => ({
      '@type': 'HowToDirection',
      text: item,
    })),
  })),
};
---

<Layout
  title="Onze aanpak – AK Web Solutions"
  description="Ontdek hoe AK Web Solutions digitale trajecten uitvoert: van discovery en design tot development en groei in tweewekelijkse sprints."
  structuredData={[breadcrumbSchema, howToSchema, faqSchema]}
>
  <Header />
  <main>
    <section class="approach-hero">
      <div class="approach-hero__bg" aria-hidden="true">
        <span class="approach-hero__grid"></span>
        <span class="approach-hero__orb approach-hero__orb--left"></span>
        <span class="approach-hero__orb approach-hero__orb--right"></span>
        <span class="approach-hero__flare"></span>
      </div>
      <div class="approach-hero__shell">
        <div class="approach-hero__intro" data-reveal>
          <span class="approach-hero__tag">Onze aanpak</span>
          <h1>Samen in sprints bouwen aan een digitale groeimachine.</h1>
          <p>We werken in ritmes van twee weken. Iedere sprint levert iets tastbaars op: design, development, campagnes of learnings. Geen ruis, wel momentum.</p>
          <div class="approach-hero__actions">
            <a class="hero-btn hero-btn--primary" href="/contact">Plan een werksessie</a>
            <a class="hero-btn hero-btn--ghost" href="/cases">Bekijk resultaten</a>
          </div>
        </div>
        <div class="approach-hero__stats">
          {approachStats.map((metric, index) => (
            <div class="approach-hero__stat" data-reveal style={`--reveal-delay: ${index * 0.08}s`}>
              <strong>{metric.value}</strong>
              <span>{metric.label}</span>
            </div>
          ))}
        </div>
        <div class="approach-hero__panel">
          <div class="approach-hero__cards">
            {approachHighlights.map((item, index) => (
              <article class="approach-hero__card tilt-card" data-reveal style={`--reveal-delay: ${index * 0.1}s`}>
                <span class="approach-hero__card-icon">
                  <IconBadge set={item.icon.set} name={item.icon.name} width={24} color="rgba(244,244,255,0.92)" />
                </span>
                <h3>{item.title}</h3>
                <p>{item.body}</p>
              </article>
            ))}
          </div>
          <div class="approach-hero__timeline">
            {approachTimeline.map((stage, index) => (
              <div class="approach-hero__timeline-item" data-reveal style={`--reveal-delay: ${index * 0.08 + 0.1}s`}>
                <span class="approach-hero__timeline-dot"></span>
                <div>
                  <span class="approach-hero__timeline-step">{stage.step}</span>
                  <h4>{stage.title}</h4>
                  <p>{stage.body}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <section class="pulse-section">
      <div class="pulse-shell">
        <header class="pulse-intro" data-reveal>
          <span class="pulse-eyebrow">Sprintfases</span>
          <h2 class="pulse-title">Onze aanpak is iteratief, transparant en altijd meetbaar.</h2>
          <p class="pulse-copy">Vier fases die telkens terugkomen. Zo bouwen we razendsnel een solide fundament, valideren we oplossingen en schalen we campagnes zodra ze werken.</p>
        </header>
        <div class="pulse-grid pulse-grid--3">
          {phases.map((phase, index) => (
            <article class="pulse-card tilt-card" data-reveal style={`--reveal-delay: ${index * 0.08}s`} id={phase.title.toLowerCase()}>
              <span class="pulse-pill">Fase 0{index + 1}</span>
              <h3 class="pulse-card__title">{phase.title}</h3>
              <p class="pulse-card__body">{phase.description}</p>
              <ul class="pulse-list">
                {phase.deliverables.map((item) => (
                  <li>{item}</li>
                ))}
              </ul>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="pulse-section">
      <div class="pulse-shell">
        <header class="pulse-intro" data-reveal>
          <span class="pulse-eyebrow">Sprint cadans</span>
          <h2 class="pulse-title">Iedere twee weken een duidelijke release + growth acties.</h2>
          <p class="pulse-copy">We werken in vaste loops. Zo weet je wanneer beslissingen vallen, welke deliverables live gaan en welk experiment we klaarzetten voor de volgende sprint.</p>
        </header>
        <div class="pulse-grid pulse-grid--3">
          {rhythm.map((step, index) => (
            <article class="pulse-card tilt-card" data-reveal style={`--reveal-delay: ${index * 0.08}s`}>
              <span class="pulse-pill">{step.label}</span>
              <h3 class="pulse-card__title">{step.title}</h3>
              <p class="pulse-card__body">{step.body}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="pulse-section">
      <div class="pulse-shell">
        <header class="pulse-intro" data-reveal>
          <span class="pulse-eyebrow">Belofte</span>
          <h2 class="pulse-title">Dit kun je verwachten van elke samenwerking.</h2>
          <p class="pulse-copy">We werken als verlengstuk van je team. Transparant, pragmatisch en altijd gericht op momentum in je groeidoelen.</p>
        </header>
        <div class="pulse-grid pulse-grid--3">
          {guarantees.map((item, index) => (
            <article class="pulse-card tilt-card" data-reveal style={`--reveal-delay: ${index * 0.1}s`}>
              <h3 class="pulse-card__title">{item.title}</h3>
              <p class="pulse-card__body">{item.body}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="pulse-section">
      <div class="pulse-shell">
        <header class="pulse-intro" data-reveal>
          <span class="pulse-eyebrow">FAQ</span>
          <h2 class="pulse-title">Veelgestelde vragen over onze aanpak.</h2>
          <p class="pulse-copy">Heldere verwachtingen zorgen voor snelheid. Hieronder vind je de antwoorden op vragen die we vaak krijgen voordat we starten.</p>
        </header>
        <div class="faq-list">
          {faqs.map((item, index) => (
            <details class="faq-item" data-reveal style={`--reveal-delay: ${index * 0.1}s`}>
              <summary>{item.question}</summary>
              <p>{item.answer}</p>
            </details>
          ))}
        </div>
      </div>
    </section>

    <section class="pulse-section">
      <div class="pulse-shell">
        <article class="pulse-cta" data-reveal>
          <span class="pulse-eyebrow">Start nu</span>
          <h2 class="pulse-title">Kickstart je eerste sprint.</h2>
          <p class="pulse-copy">We plannen een werksessie waarin we scope, KPI’s en prioriteiten vastleggen. Na 45 minuten weet je precies hoe de eerste maand eruit ziet.</p>
          <div class="pulse-cta__actions">
            <a href="/contact" class="hero-btn hero-btn--primary">Plan een sessie</a>
            <a href="mailto:info@akwebsolutions.nl" class="hero-btn hero-btn--ghost">Mail ons</a>
          </div>
        </article>
      </div>
    </section>
  </main>
  <Footer />
</Layout>
